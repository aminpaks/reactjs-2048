import Color from 'color';

const primary = Color('#457b9d');
const secondary = Color('#e63946');
const tertiary = Color('#a8dadc');
const quandary = Color('#f1faee');
const textContrast = Color('#fff');

const shade = Color('#999');
const white = Color('#fff');

export const palette = {
  primary,
  quandary,
  secondary,
  shade,
  tertiary,
  textContrast,
  white,
};
